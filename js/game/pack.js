"use strict";

var app = app || {};

app.initPack = function(player, tab){
	var p = player.pack = [];

	var rows = 4;
	var cols = 3;

	// generate pack array
	for (var r = 0; r < rows; r++) {
		p.push([]);
		for (var c = 0; c < cols; c++) {
			p[r].push(undefined);
		};	
	};

	






	// items




}